# Bounded local sequence acceptance

Proceed only from the exact green Actions commit in SEQUENCE_ACCEPTANCE.md.
Use a separate worktree, unchanged installed skill, registered Python/Blender and
existing private library/evidence. All automated development tests run in Actions;
this is a controlled real-source acceptance, not another local development suite.

1. Read AGENTS.md, CURRENT TOP of LOCAL_MOTION_SESSION_HANDOFF.md,
   REVIEWED_SEQUENCES.md and references/sequences.md. Inspect worktree state;
   other agents may be active. No reset/clean/merge/release/global install.
2. Reuse authorized Moonwalk and actual Male Adventurer, not the earlier
   Quaternius mannequin or a new proxy. Reuse hashes and prior outputs. Do not
   acquire replacements, scan broad disk roots or touch Desert Warrior.
3. The user attested Thriller Part 4 came from official Adobe Mixamo. Verify its
   current hash and record the file-specific grant through the existing retained-
   evidence policy path. The prior current-files-only grant does not cover it
   automatically. No blanket approval of future files and no asset upload to CI.
4. Inspect both source clips and target. Source fingerprints differ despite the
   generic action labels. Keep animation and character provenance separate.
   Preserve target mesh/weights/rest, 21 prior actions, root control and display.
   Work from a new saved target copy, never an unrelated unsaved live window.
5. Reuse a completed reviewed Moonwalk retarget as DATA when job/outputs/proposal/
   roles and grants verify. Never rewrite or rerun its old job under new code.
   If its required reviewed context is absent, report it and replan explicitly.
6. Audit Thriller independently. Review its own mapping, units, facing and anchor.
   Retain its entire observed range at native speed. Request an explicit adequate
   max_output_intervals budget. Do not crop, reduce sampling or speed up to pass.
   No automatic blanket grounding of the long dance or changes to target anatomy.
7. Both resulting actions must address the same target rest/world, translation
   anchor and complete keyed-bone coverage. Inspect missing fingers; do not invent
   them or rewrite old clips. Explain any actual incompatibility before proceeding.
8. Prepare sequence-plan from completed retarget JOB IDs. Choose bridge duration,
   yaw, placement and bounded work based on observed endpoints, not fixture values.
   Preserve full A + extra bridge + full B. Inspect proposed displacement, pose and
   velocity evidence, then explicitly approve that exact plan and execute once.
9. Run sequence-check on exact result bytes before presentation. Review intended/
   stored endpoints, native seconds, endpoint and seam metrics, calibrated integer/
   subframe soles and both-source license lineage. Report penetration or implausible
   motion; do not bypass guards, lock gliding feet or secretly edit animation keys.
10. Reuse suitable floor/camera/lights/display, or create a separate reviewed
    presentation derivative. At most eight CPU stills, 640x360/16 samples total.
    No full-video render is authorized by this preview budget. Numeric PASS does
    not overrule a visual defect.
11. Present the result in a separate, explicitly bound Blender review window using
    only an available approved host route. Show the whole native-speed sequence and
    join. If control is unavailable, give the exact file and manual playback steps.
    Do not change ports, configuration, preferences, other windows or install a
    generic command service. Do not claim temporal viewing from frame advancement.
12. Verify source/prior-output/installed-skill/config preservation. Do not restore
    unrelated agent changes or erase the unresolved historical shared-state gaps.

Report commit/runtime, asset/target hashes, grant state, completed jobs, exact plan/
review/sequence IDs, mappings/units/anchor, full ranges/FPS/seconds, bridge duration,
placement, measured seam/contact evidence, rig/skin/action/display preservation,
exact output paths and viewing scope. Stop on a defect with bounded evidence.

TECHNICAL: PASS / FAIL
SEQUENCE CONTINUITY: PASS / WARN / FAIL (state measured criteria)
PERFORMANCE: PENDING unless actual temporal evidence was inspected
HUMAN: only the actual user decision bound to output hash and viewing scope

No repository patch, merge, release, global installation, production overwrite,
GPU reconstruction or full IK as part of acceptance.
